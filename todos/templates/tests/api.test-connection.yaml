apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todos.fullname" . }}-test-connection"
  labels:
{{ include "todos.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "todos.fullname" . }}-api:{{ .Values.api.service.port }}']
  restartPolicy: Never
